# Architecture

Placeholder — expand into a narrative/diagram version of the layered architecture described in `PROMPT.md` §3 (Client → API/Edge → AI Engines → Processing → Data Stores, plus the standalone coupon-scraper service) as the system is built.
