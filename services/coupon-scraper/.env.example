# Copy to: services/coupon-scraper/.env
# Values below match the team's shared .env (see shooting-star.env in the group chat).
# Never commit the real .env - it's already covered by the repo's .gitignore.

DATABASE_URL=postgresql+psycopg://postgres.exxrlklhttekqjzdtmag:SS%40ht131629@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Leave unset (or delete this line) to fall back to a local SQLite file for
# standalone testing without touching the shared Supabase DB.
