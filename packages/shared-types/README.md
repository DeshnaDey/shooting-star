# packages/shared-types

Types/interfaces shared between the web app and any Node-based tooling (e.g. quiz/question schemas, analysis result shapes, points ledger events). Keep in sync with the API's Pydantic schemas.
